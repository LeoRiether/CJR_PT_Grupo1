<!DOCTYPE html>
<html lang="en">
    <head>
        <title>Error!</title>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
    </head>
    <body>

        <h1>Something went wrong!</h1>
        <h2>{{ status | append: ": " }}{{ message }}</h2>
        <pre>{{ error.stack }}</pre>

        <style>
            body {
                display: flex;
                flex-direction: column;
                min-height: 100vh;
                font-family: sans-serif;
                margin: 0;
                padding: 0;
            }
            h1 {
                background-color: #A91409;
                color: white;
                margin: 0;
                padding: 20px;
            }
            h2 {
                background-color: #111E3E;
                color: #eee;
                margin: 0;
                padding: 20px;
            }
            pre {
                background-color: #222E50;
                color: white;
                margin: 0;
                padding: 20px;
                flex-grow: 1;
            }
        </style>
    
    </body>
</html>

